name: publish

on: [push]

jobs:
  publish-laravel-image:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build the laravel Docker image
      run: |
        echo $CR_PAT | docker login ghcr.io -u info@nathanjansen.nl --password-stdin